1. static_cast
   derived class의 포인터를 base class로 캐스팅, 그 반대의 경우도 사용가능
   런타임 체크를 하지않기 때문에 안전하지 않다.
   
2. const_cast
   타입에 const 키워드를 추가하거나 삭제할때 사용
   컴파일 시에 타입 확인
   
3. dynamic_cast
   객체의 포인터에만 사용
   해당 객체를 상속관계 속에서 캐스팅할 때 사용








