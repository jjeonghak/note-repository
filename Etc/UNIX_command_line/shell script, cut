//cut : 파일 내욜을 각 필드로 구분하고 필드별 내용 추출
      -d ' ' -f1 : 공백을 기준으로 필드를 구분하고 첫번쨰 필드값 추출, 공백이 긴 경우 사용제한
      -c 3, 1 : 캐릭터 단위, 리스트의 첫번째와 세번째 인덱스 추출(단, 순서는 인덱스 순으로)
      * -c1-5(1에서 5까지) -c-5(처음부터 5까지) -c5-(5부터 끝까지)
      -b : 바이트 단위, 문자에 적용불가

//awk : Aho Weinberger Kernighan, 유닉스에서 개발된 스크립트 언어
//패턴검색, 산술연산, 변수선언, 배열, 조건문, 반복문
awk '{ print $0 }' : 모든 필드 추출
awk '{ print $1 }' : 공백이 길어도 필드 별로 구분하고 추출


