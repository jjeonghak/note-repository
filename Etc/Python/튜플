# 튜플(tuple)
리스트와 달리 원소값을 직접 변경할 수 없고 문자열처럼 사용

t = ()  #튜플은 괄호(비어있을 때)로 선언
t = a,  #또는 콤마로 선언
t = (a,)
t = ('a', 'b', 'c')

a = list(b)
c = tuple(a)  #리스트와 토플 서로 형변환 가능

a = 1
b = 2
a, b = b, a  #임시로 값을 저장한는 temp 변수 필요없음
print(a,b)
- 결과 : 2 1

def all_print(a, b, *c):  #파라미터를 세개 이상 받을 수 있음, 나머지 파라미터는 튜플로 
    print x, y, c

all_print(1, 2, 3, 4, 5)
- 결과 : 1 2 (3, 4, 5)
