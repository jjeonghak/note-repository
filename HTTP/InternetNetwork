//IP(Internet Protocol)
  네트워크 계층
  지정한 IP address에 인터넷 망을 통해 데이터 전달
  패킷이라는 통신 단위로 데이터 전달
  IP 패킷 정보 : 출발지 IP, 목적지 IP, 전송 데이터 등
  
  IP 프로토콜 한계
    비연결성 : 패킷을 받을 대상이 없거나 서비스 불능 상태여도 패킷 전송
    비신뢰성 : 패킷이 중간에 소실되거나 분리순서에 맞지않게 도착
    프로그램 구분 : 같은 IP를 사용하는 서버에서 통신하는 애플리케이션이 여러개인 경우


//TCP(Transmission Control Protocol)
  전송 계층의 전송 제어 프로토콜
  기존 IP 프로토콜의 한계 보완
  TCP 3 way handshake(가상 연결)를 통한 물리적이 아닌 논리적인 연결지향적
    SYN : 접속 요청
    ACK : 요청 수락
      1. 클라이언트는 서버에게 SYN 전송
      2. 서버는 클라이언트에게 전송받은 SYN와 추가적인 ACK 전송
      3. 클라이언트는 서버에게 전송받은 ACK와 데이터를 전송
      4. 서버는 TCP 패킷 정보를 확인 후 데이터 신뢰성 확인
  
  신뢰할 수 있는 프로토콜 : 데이터 전달 보증, 분리순서 보장
  IP 패킷 정보에 추가적으로 출발지 PORT, 목적지 PORT, 전공 제어, 순서, 검증 정보가 추가로 포함
  

//UDP(User Datagram Protocol)
  TCP와 같은 전송 계층
  하지만 IP 프로토콜과 좀더 유사
  IP 패킷 정보에 추가적으로 출발지 PORT, 목적지 PORT, 체크섬 추가로 포함
  수정 불가능한 TCP와는 다르게 UDP는 수정가능(최적화 가능)


//PORT


//DNS

