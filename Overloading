// 함수의 오버로딩(Overloading)는 C++에서 같은 이름을 가진 함수가 여러개 존재하는 것
// 함수를 호출했을 때 사용하는 인자를 보고 함수를 결정

1) 자신과 타입이 정확히 일치하는 함수를 찾는다.
2) 정확히 일치하는 타입이 없는 경우 형변환을 통해 일치하는 함수를 찾는다.
 * char, unsigned char, short는 int로 변환
 * unsigned short는 int의 크기에 따라 int 혹은 unsigned int로 변환
 * float은 double로 변환
 * enum은 int로 변환
3) 위와 같이 변환해도 일치하지 않을 경우 좀더 포괄적인 형변환
 * 임의의 숫자 타입은 다른 숫자 타입으로 변환(float -> int)
 * enum도 임의의 숫자 타입으로 변환(enum -> double)
 * 0은 포인터 타입이나 숫자 타입으로 변환
 * 포인터는 void 포인터로 변환
4) 유지 정의된 타입 변환으로 일치하는 것을 찾는다.

위 과정을 통하더라도 일치하는 함수를 찾을 수 없거나 
같은 단계에서 두개 이상이 일치하는 경우 모호하다(Ambiguous)로 판단, 오류발생
