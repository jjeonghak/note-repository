# Alias : 가명 
변수 = 객체일때, 객체에 변수라는 가명을 정해준 것
이렇게 객체와 변수의 연관관계를 참조(Reference)라고 한다.

x = 1
y = x  #y라는 가명
y = 5  #가명 y가 새로운 객체에 참조됨
print(x, y)
- 결과 : 1 5

x = [1, 2, 3]
y = x
y[0] = 4  #가명 y가 가리키는 x 변경
print(x)
print(y)
- 결과 : [4, 2, 3]
        [4, 2, 3]
        
x = [1, 2, 3]
y = list(x)  #가명이 아닌 새로운 객체 선언
y[0] = 4  
print(x)
print(y)
- 결과 : [1, 2, 3]
        [4, 2, 3]        

