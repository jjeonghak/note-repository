# 세트(Set)
# 중복데이터 삽입 불가
# 래덤정렬, 정렬 순서없음
# 탐색 및 삭제 가능
# 해시테이블로 구현, key값만 이용
# 중복이 없는 데이터를 받는 경우 리스트보다 수행시간이 빠름

# 시간 복잡도
탐색 : O(1) 평균
삽입 : O(1) 평균
삭제 : O(1) 평균
길이 확인 : O(1)

set_data = set()  #set선언

set_data.add(data)  #데이터 삽입

set_data.remove(data)  #데이터 삭제
